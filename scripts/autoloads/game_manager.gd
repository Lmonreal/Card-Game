extends Node

#const CARD_SCENE : PackedScene = preload("res://scenes/ui/card_visual.tscn")
#var deck = DeckFactory.build_deck()
#
#func _ready() -> void:
	#for i in range(3):
		#var card_visual = CARD_SCENE.instantiate()
		#card_visual.card_data = deck.get(i)
		#card_visual.position.y += 50
		#card_visual.position.x += i * 60
		#card_visual.scale = Vector2(2,2)
		#add_child(card_visual)
